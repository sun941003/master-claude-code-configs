# MoonDeveloper — Toolchain Strategy

> 개발 도구, CI/CD, 자동화, 하드웨어 전략.
> 목표: "내가 직접 확인/판단해야 하는 일을 최소화"

---

## 도구 역할 분담

| 도구 | 용도 | 토큰/비용 |
|------|------|----------|
| **Claude (채팅)** | 기획, 문서, 아키텍처 설계, 전략 | Max $200/월 |
| **Claude Code** | 코드 구현, git, 빌드, 테스트 실행 | Max 공유 |
| **JetBrains (AS/Fleet)** | UI 디버깅, 프로파일링, 수동 테스트, 실기기 | 월 구독 |
| **터미널/스크립트** | 반복 작업, 빌드, 배포, 정리 | 무료 |
| **슬랙** | 배포 알림, CI 결과, 원클릭 승인 | 무료 워크스페이스 |

### 작업 분산 판단 기준

```
Claude Code 사용:
  ∙ 코드 작성/수정/리팩토링
  ∙ 테스트 작성 및 실행
  ∙ git 작업 (브랜치, 커밋, 머지)
  ∙ 빌드 검증
  ∙ 문서/스크립트 생성

IDE(AS/Fleet) 전환 제안:
  ∙ 동일 에러 3회 이상 반복 시
  ∙ UI 레이아웃 디버깅 (Preview, Inspector)
  ∙ 실기기 테스트
  ∙ 프로파일링 (메모리, CPU, 네트워크)

터미널 직접 실행 제안:
  ∙ 스크립트 한 줄로 해결되는 작업
  ∙ 빌드 캐시 정리 등 단순 반복
  ∙ 토큰 대비 비효율적인 단순 작업
```

---

## CI/CD 파이프라인

### 현재 상태

| 워크플로우 | 트리거 | 내용 | 상태 |
|-----------|--------|------|------|
| Android CI | dev push | desktopTest → assembleRelease | ✅ |
| iOS CI | release tag | Archive 빌드 | ✅ (10x 과금 주의) |
| Web CI | dev push | npm build → Firebase Hosting | ✅ |
| GitHub Release | tag push | 아티팩트 업로드 | ✅ |

### 목표 파이프라인 (Phase 12+)

```
[Layer 2 — dev push 시 자동]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
트리거: push to dev
러너: ubuntu-latest (무료)

Steps:
  1. desktopTest 전체 (5분 타임아웃)
  2. assembleRelease (빌드 성공 확인)
  3. 웹 빌드 + 웹 테스트
  4. Lint/Warning 체크
  5. 결과 → 슬랙 알림

[Layer 3 — 릴리즈 전 종합]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
트리거: workflow_dispatch (수동) 또는 release/* push

Steps:
  1. Layer 2 전체 +
  2. 보안 감사 (verify_build_security.sh)
  3. i18n 검증 (i18n-validate.py)
  4. APK 크기 체크
  5. 번들 분석
  6. 결과 리포트 → 슬랙

[배포 파이프라인 — 최종 목표]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
트리거: 슬랙 버튼 클릭 또는 tag push

Steps:
  1. AAB 빌드 → Google Play 업로드 (Fastlane)
  2. IPA 빌드 → TestFlight 업로드 (Fastlane)
  3. 웹 빌드 → Firebase Hosting 배포
  4. 슬랙 완료 알림 + 스토어 링크
```

### GitHub Actions 비용 관리

```
월 3000분 (Pro 플랜)
∙ Ubuntu: 1배 과금 → ~3000분 사용 가능
∙ macOS: 10배 과금 → ~300분만 사용 가능

전략:
∙ 일반 CI (테스트/빌드): Ubuntu only
∙ iOS 빌드: release tag 시에만 macOS (또는 로컬)
∙ 3월 1일 리셋 — 월말 분 잔여량 확인
∙ 장기: Self-hosted runner (Mac Mini 도입 시)
```

---

## Fastlane 자동화

### 현재 활용 범위

```
✅ 사용 중:
  ∙ 스토어 메타데이터 업로드 (설명문, 스크린샷)
  ∙ 다국어 설명문 관리

⬜ 확대 예정:
  ∙ Android AAB 빌드 + Play Console 업로드
  ∙ iOS Archive + TestFlight 업로드
  ∙ 스크린샷 자동 생성 (Screengrab/Snapshot)
  ∙ 버전 범프 자동화
  ∙ 코드 서명 관리 (Match)
```

### Fastlane 가능 영역 (전수 조사 대상)

```
[Google Play]
∙ supply: 메타데이터, 스크린샷, APK/AAB 업로드
∙ screengrab: 자동 스크린샷 캡처
∙ 내부/비공개/프로덕션 트랙 관리
∙ 출시 노트 자동 업데이트

[App Store]
∙ deliver: 메타데이터, 스크린샷, 빌드 업로드
∙ snapshot: 자동 스크린샷 캡처
∙ pilot: TestFlight 관리
∙ match: 인증서/프로비저닝 자동 관리
∙ gym: iOS 빌드

[공통]
∙ increment_version: 버전 관리
∙ slack: 빌드 결과 알림
```

---

## 슬랙 연동

### 설정 (워크스페이스 생성 완료)

```
[Webhook 설정]
1. Slack API → Your Apps → Create New App
2. Incoming Webhooks 활성화
3. 채널별 Webhook URL 생성:
   ∙ #ci-results  — CI/CD 결과
   ∙ #deployments — 배포 알림
   ∙ #alerts      — 긴급 알림 (크래시 등)

[GitHub Actions → 슬랙]
  - name: Notify Slack
    uses: slackapi/slack-github-action@v2
    with:
      webhook: ${{ secrets.SLACK_WEBHOOK_CI }}
      payload: |
        {"text": "✅ Splitly CI passed (${GITHUB_SHA:0:7})"}

[Interactive Buttons — 최종 목표]
∙ "🚀 프로덕션 배포" 버튼 → GitHub Actions workflow_dispatch 트리거
∙ "🧪 내부 테스트" 버튼 → 내부 테스트 트랙 배포
∙ Slack Bolt (Node.js) 또는 슬랙 워크플로우 빌더로 구현
```

---

## Claude ↔ Claude Code 동기화

### 현재 문제

- 새 대화 열면 이전 작업 컨텍스트 끊김
- MD 파일 동기화를 수동으로 챙겨야 함
- CC 작업 진행률/루프 여부 확인이 어려움

### 해결 — 3단계

```
━━ 단계 1: 즉시 적용 (완료 ✅) ━━

∙ SESSION_LOG.md 템플릿 생성 (커밋 4726a0d)
∙ KNOWN_ISSUES.md 템플릿 생성
∙ LESSONS_LEARNED.md 템플릿 생성
∙ CLAUDE.md에 세션 관리 + 테스트 규칙 추가

━━ 단계 2: MCP 서버 (중기) ━━

∙ 파일 시스템 MCP → Claude(채팅)에서 프로젝트 파일 직접 읽기
∙ GitHub MCP → PR/이슈/커밋 히스토리 참조
∙ 이렇게 하면 채팅에서 CC 작업 결과를 실시간 확인 가능

━━ 단계 3: 자동화 (장기) ━━

∙ CC 작업 완료 → 슬랙 요약 알림
∙ 배포 결과 → 슬랙 (Interactive Buttons)
∙ Cowork 연동 시 동일 문서 체계 자동 적용
```

### CC 루프 방지 전략

```
1. 모든 CC 프롬프트에 타임아웃 명시
   "컴파일 5분 초과 시 gradle --stop 후 재시도 1회, 재실패 시 중단"

2. 체크포인트 기반 진행
   "Step 1 완료 후 결과 출력, Step 2 진행 전 확인"

3. 3-failure rule 적용
   "3회 실패 시 @Ignore + TODO, 강제 진행"

4. SESSION_LOG에 진행률 기록
   CC가 작업 중 주요 단계마다 SESSION_LOG 업데이트
```

---

## 자동화 확장 (맥미니 불필요, 마이그레이션 비용 0)

> 모두 GitHub/클라우드 기반. 맥미니 도입 시 runner만 교체하면 끝.

### 1. Git Hooks — 푸시 전 자동 검증

```
pre-commit:  Lint + 포맷 체크 (1-2초)
pre-push:    desktopTest 실행 (실패 시 푸시 차단)

도구: lefthook (Kotlin/멀티플랫폼 친화)
설정: lefthook.yml → 프로젝트 루트
```

### 2. Gradle Remote Build Cache

```
빌드 캐시를 GitHub Actions ↔ 로컬 간 공유.
한 번 컴파일한 모듈은 재컴파일 하지 않음.

설정: gradle.properties + GitHub Actions cache action
효과: CI 빌드 시간 30-50% 단축
```

### 3. Fastlane 배포 파이프라인

```
[Android — Ubuntu runner]
tag push → fastlane supply → Play Console 내부테스트 트랙 자동 업로드

[웹]
dev push → npm build → firebase deploy (이미 구축)

[iOS — 로컬 Fastlane → 맥미니 도입 시 Self-hosted runner]
fastlane gym → pilot → TestFlight 업로드

Fastfile은 환경 불문 동일. runner만 교체.
```

### 4. Firebase App Distribution

```
Play Console 비공개 테스트 14일 대기 없이:
∙ APK를 Firebase App Distribution으로 즉시 배포
∙ 테스터 이메일 등록 → 설치 링크 자동 발송
∙ Fastlane firebase_app_distribution 플러그인 연동
```

### 5. Danger JS — PR 자동 리뷰

```
PR 열 때 자동 체크:
∙ 테스트 미추가 경고
∙ 번역 키 누락 감지
∙ STUB 추가 시 QA 체크리스트 업데이트 알림
∙ APK 크기 변화 리포트

GitHub Actions에서 실행, 결과를 PR 코멘트로 표시.
```

### 6. Renovate Bot — 의존성 자동 업데이트

```
∙ 마이너/패치: CI 통과 시 자동 머지
∙ 메이저: PR만 생성, 수동 확인
∙ jest 30 같은 건 PR로 알려주고, Koin 패치는 자동 처리
```

### 적용 우선순위

| # | 자동화 | 효과 | 난이도 |
|---|--------|------|--------|
| 1 | Fastlane 배포 | 배포 시간 90% 절감 | 중 |
| 2 | Gradle Build Cache | 빌드 30-50% 단축 | 저 |
| 3 | Firebase App Distribution | 테스터 배포 즉시화 | 저 |
| 4 | Git Hooks (lefthook) | 깨진 코드 방지 | 저 |
| 5 | Danger JS | PR 품질 자동 관리 | 중 |
| 6 | Renovate Bot | 의존성 관리 자동화 | 저 |

---

## 하드웨어 & SSD 관리

### 현재 환경

```
MacBook Air M4 / 24GB RAM / 256GB SSD
∙ 칩 성능: ✅ 충분
∙ RAM: ✅ 일반 개발 OK (멀티 IDE 시 빠듯)
∙ SSD: ❌ 256GB 심각하게 부족
∙ 보유 장비: Jump Station (원격 접근)
```

### SSD 경량화 (cleanup_dev.sh 구축 완료 ✅)

```
정리 대상:
∙ ~/.gradle/caches/ (transforms, build-cache)
∙ ~/Library/Developer/Xcode/DerivedData/
∙ ~/Library/Developer/CoreSimulator/
∙ ~/Library/Android/sdk/system-images/ (불필요 API 삭제)
∙ npm cache, CocoaPods cache, Homebrew cache
∙ .DS_Store 파일

실행: ~/scripts/cleanup_dev.sh
주기: 주 1회 (cron 설정 권장)
```

### 하드웨어 투자 로드맵

```
단계 0 — 무비용 (완료 ✅)
∙ cleanup_dev.sh로 30-60GB 회수

단계 1 — 소비용 (~₩50,000, 월 수익 발생 시)
∙ 외장 SSD 1TB
∙ .gradle/caches, DerivedData 심링크

단계 2 — 중비용 (~₩1.5M-2.5M, 월 $2K+ 시)
∙ Mac Mini M4 Pro (RAM 32GB, SSD 1TB+)
∙ 용도: 빌드 서버 + 원격 개발 + 저장소
∙ VSCode SSH Remote + Jump Station
∙ Self-hosted GitHub Actions runner

단계 3 — 고비용 (월 $5K+ 시)
∙ UPS, 추가 저장소, 모니터
```

---

## 보안 & 시크릿 관리

### 방식: git-crypt + 프라이빗 레포

```
moondeveloper-secrets/ (프라이빗)
├── apple/          # Team ID, ASC Key, p12
├── google/         # 서비스 계정, keystore
├── firebase/       # 프로젝트별 설정
├── admob/          # App ID
├── github/         # PAT
├── slack/          # Webhook URLs
└── scripts/
    ├── load_secrets.sh
    └── verify_secrets.sh
```

### 사용법

```bash
# 복호화 (1회)
git-crypt unlock

# 환경변수 로드
source ~/moondeveloper-secrets/apple/team_id.env

# Fastlane에서 사용
fastlane deliver --api_key_path $ASC_KEY_PATH

# CI에서 사용
# GitHub Secrets에 git-crypt 키 등록
# workflow에서 unlock → source → 빌드
```

### 관리 대상

```
[Apple]  Team ID, ASC Key ID/Issuer, p12 비밀번호, 인증서 만료일
[Google] 서비스 계정 JSON, 키스토어 경로/비밀번호/별칭
[Firebase] 프로젝트 ID (splitly-aea8c, ureen-6ae77)
[AdMob] Android/iOS App ID
[GitHub] PAT
[Slack] Webhook URLs
```

---

## 관련 문서

- [VISION.md](VISION.md) — 비전, 목표, 원칙
- [DEVELOPMENT_GUIDE.md](DEVELOPMENT_GUIDE.md) — 개발 표준, 테스트, 코드 컨벤션
- [LIBRARY_STRATEGY.md](LIBRARY_STRATEGY.md) — 모듈화, 오픈소스, 다국어

